<!doctype html>
<html lang="pt-BR">
<head>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width,initial-scale=1">
<title>PlatiniunNet</title>
<style>
body{font-family:Arial,sans-serif;margin:0;background:#f5f6f8;color:#222}
header{background:#8f0a57;color:#fff;padding:18px}
nav{display:flex;flex-wrap:wrap;gap:6px;padding:10px;background:#fff;position:sticky;top:0}
nav button{padding:10px;border:0;border-radius:8px;cursor:pointer}
nav button.active{background:#8f0a57;color:#fff}
main{max-width:1200px;margin:auto;padding:16px}
.page{display:none}.page.active{display:block}
.card{background:#fff;padding:16px;border-radius:12px;margin-bottom:14px}
.grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(220px,1fr));gap:10px}
input,select,textarea{width:100%;padding:10px;border:1px solid #ccc;border-radius:8px}
button.primary{background:#8f0a57;color:#fff}
table{width:100%;border-collapse:collapse;background:#fff}
th,td{padding:10px;border-bottom:1px solid #eee;text-align:left}
.badge{padding:4px 8px;border-radius:20px;background:#eee}.ok{background:#e5f7ec;color:#138a4b}.bad{background:#ffebee;color:#c62828}
.muted{color:#667085}

.modal{display:none;position:fixed;inset:0;background:#0008;z-index:100;align-items:center;justify-content:center;padding:16px}
.modal.open{display:flex}
.modal-box{background:#fff;width:min(760px,100%);max-height:90vh;overflow:auto;border-radius:14px;padding:20px;box-shadow:0 20px 60px #0005}
.modal-actions{display:flex;gap:8px;justify-content:flex-end;margin-top:16px}

.client-main-grid{display:grid;grid-template-columns:repeat(12,minmax(0,1fr));gap:10px;align-items:end}
.client-main-grid>div{grid-column:span 3}
.client-main-grid .address-field{grid-column:span 5}
.client-main-grid .compact-field{grid-column:span 2}
.client-main-grid .compact-field input{width:100%;box-sizing:border-box}
@media(max-width:900px){
  .client-main-grid>div{grid-column:span 6}
  .client-main-grid .address-field{grid-column:span 8}
  .client-main-grid .compact-field{grid-column:span 4}
}
@media(max-width:600px){
  .client-main-grid{grid-template-columns:1fr}
  .client-main-grid>div,.client-main-grid .address-field,.client-main-grid .compact-field{grid-column:1}
}

.campo-pendente{border:2px solid #c62828!important;background:#fff4f4!important}
.correcao-aviso{padding:12px;border-radius:8px;background:#fff3cd;border:1px solid #ffe69c;margin-bottom:14px}

.dashboard-v3{display:grid;gap:18px}
.dash-kpis{display:grid;grid-template-columns:repeat(auto-fit,minmax(175px,1fr));gap:12px}
.dash-kpi{background:#fff;border:1px solid #e7e7ec;border-radius:14px;padding:16px;box-shadow:0 2px 8px rgba(0,0,0,.035)}
.dash-kpi.clickable{cursor:pointer;transition:.15s ease}.dash-kpi.clickable:hover{border-color:#8f0a57;transform:translateY(-1px)}
.dash-kpi .kpi-label{font-size:12px;color:#6b6b75;text-transform:uppercase;letter-spacing:.04em}
.dash-kpi .kpi-value{font-size:27px;font-weight:800;margin-top:6px}
.dash-kpi .kpi-sub{font-size:12px;color:#777;margin-top:4px}
.dash-sections{display:grid;grid-template-columns:repeat(auto-fit,minmax(310px,1fr));gap:14px}
.dash-panel{background:#fff;border:1px solid #e7e7ec;border-radius:14px;padding:16px}
.dash-panel h3{margin:0 0 12px}
.dash-row{display:flex;justify-content:space-between;gap:12px;padding:9px 0;border-bottom:1px solid #eee}
.dash-row:last-child{border-bottom:0}
.dash-number{font-weight:700;white-space:nowrap}
.dash-alert-link{cursor:pointer}


.permission-grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(220px,1fr));gap:8px}
.permission-item{display:flex;align-items:center;gap:8px;padding:9px;border:1px solid #e5e5ea;border-radius:8px;background:#fff}
.permission-item input{width:auto}
.permission-section{grid-column:1/-1;margin-top:8px;font-weight:700;color:#555}



#loginScreen{
  display:flex;
  justify-content:center;
  align-items:flex-start;
  min-height:100vh;
  padding:12px;
  box-sizing:border-box;
  background:#000;
  overflow:auto;
}
.login-art-shell{
  position:relative;
  width:min(430px,96vw);
  aspect-ratio:2/3;
  flex:0 0 auto;
}
.login-art-shell>img{
  position:absolute;
  inset:0;
  width:100%;
  height:100%;
  display:block;
  object-fit:contain;
  user-select:none;
  pointer-events:none;
}
.login-real-input{
  position:absolute;
  left:23.5%;
  width:55.7%;
  height:5.1%;
  box-sizing:border-box;
  border:0;
  outline:0;
  background:transparent;
  color:#fff;
  font-size:clamp(13px,2.7vw,18px);
  font-family:Arial,sans-serif;
  padding:0 2%;
  z-index:3;
}
.login-real-input::placeholder{color:transparent}
#loginUsuario{ top:52.3%; }
#loginSenhaSistema{ top:66.8%; }
#loginEnterHit{
  position:absolute;
  left:20.5%;
  top:77.3%;
  width:59%;
  height:7.0%;
  border:0;
  background:transparent;
  cursor:pointer;
  z-index:4;
}
#loginEnterHit:focus{
  outline:2px solid rgba(255,20,110,.75);
  outline-offset:2px;
  border-radius:10px;
}
#loginMsg{
  position:absolute;
  left:17%;
  top:85.5%;
  width:66%;
  min-height:18px;
  text-align:center;
  color:#ff9fc1;
  font-size:11px;
  font-weight:700;
  z-index:5;
  text-shadow:0 1px 2px #000;
}
#appShell{display:none}
.userbar{display:flex;gap:10px;align-items:center;justify-content:space-between;padding:8px 14px;background:#fff;border-bottom:1px solid #eee}
.userbar .who{font-size:13px;color:#555}
.hidden-by-permission{display:none!important}
@media(max-height:760px) and (min-width:600px){
  .login-art-shell{width:min(390px,92vw)}
}


#loginScreen{
  display:flex;
  justify-content:center;
  align-items:flex-start;
  min-height:100vh;
  padding:28px 16px 40px;
  box-sizing:border-box;
  background:#050505;
}
.login-wrap{
  width:min(390px,94vw);
}
.login-brand{
  width:min(350px,92vw);
  margin:0 auto 8px;
}
.login-brand img{
  display:block;
  width:100%;
  height:auto;
  object-fit:contain;
}
.login-card{
  width:100%;
  box-sizing:border-box;
  background:#0c0d12;
  border:1px solid #f20b67;
  border-radius:18px;
  padding:20px 22px 22px;
  box-shadow:0 0 22px rgba(242,11,103,.13);
}
.login-card label{
  display:block;
  color:#fff;
  font-size:13px;
  font-weight:800;
  margin:10px 0 6px;
}
.login-card input[type="text"],
.login-card input[type="password"]{
  width:100%;
  min-height:46px;
  box-sizing:border-box;
  padding:11px 13px;
  border:1px solid #30323a;
  border-radius:9px;
  background:#15161c;
  color:#fff;
  outline:none;
  font-size:14px;
}
.login-card input::placeholder{
  color:#80808a;
}
.login-card input:focus{
  border-color:#f20b67;
  box-shadow:0 0 0 2px rgba(242,11,103,.12);
}
.login-actions{
  margin-top:17px;
}
.login-actions button{
  width:100%;
  min-height:48px;
  border:0;
  border-radius:9px;
  background:linear-gradient(90deg,#ff086b,#d90859);
  color:#fff;
  font-size:15px;
  font-weight:900;
  cursor:pointer;
}
#loginMsg{
  min-height:18px;
  margin-top:10px;
  text-align:center;
  color:#ff9abb;
  font-size:13px;
}
.login-footer{
  margin-top:10px;
  text-align:center;
  color:#8f8f98;
  font-size:11px;
}
.login-footer b{color:#ff176d}
#appShell{display:none}
@media(max-width:480px){
  #loginScreen{padding-top:14px}
  .login-wrap{width:min(350px,95vw)}
  .login-brand{width:min(320px,92vw)}
  .login-card{padding:18px}
}


#loginScreen{
  display:flex;
  justify-content:center;
  align-items:flex-start;
  min-height:100vh;
  padding:70px 16px 32px;
  box-sizing:border-box;
  background:#f4f4f7;
}
.login-wrap{
  width:min(330px,92vw);
}
.login-card{
  width:100%;
  box-sizing:border-box;
  background:#fff;
  border:1px solid #e2e2e7;
  border-radius:16px;
  padding:22px;
  box-shadow:0 8px 24px rgba(0,0,0,.08);
}
.login-title{
  margin:0 0 4px;
  text-align:center;
  font-size:24px;
  font-weight:900;
  color:#8f0a57;
}
.login-subtitle{
  margin-bottom:18px;
  text-align:center;
  font-size:12px;
  color:#777;
}
.login-card label{
  display:block;
  margin:10px 0 6px;
  font-size:13px;
  font-weight:700;
  color:#333;
}
.login-card input[type="text"],
.login-card input[type="password"]{
  width:100%;
  min-height:44px;
  box-sizing:border-box;
  padding:11px 12px;
  border:1px solid #d6d6dc;
  border-radius:9px;
  background:#fff;
  color:#222;
  outline:none;
  font-size:14px;
}
.login-card input::placeholder{
  color:#9a9aa1;
}
.login-card input:focus{
  border-color:#8f0a57;
  box-shadow:0 0 0 2px rgba(143,10,87,.10);
}
.login-actions{
  margin-top:16px;
}
.login-actions button{
  width:100%;
  min-height:44px;
  border:0;
  border-radius:9px;
  background:#8f0a57;
  color:#fff;
  font-size:15px;
  font-weight:800;
  cursor:pointer;
}
#loginMsg{
  min-height:18px;
  margin-top:10px;
  text-align:center;
  color:#a40036;
  font-size:13px;
}
.login-footer{
  margin-top:12px;
  text-align:center;
  color:#8b8b92;
  font-size:11px;
}
#appShell{display:none}
@media(max-width:480px){
  #loginScreen{padding-top:42px}
  .login-wrap{width:min(310px,92vw)}
  .login-card{padding:18px}
}


.dash-panel.clickable{cursor:pointer;transition:.15s ease}
.dash-panel.clickable:hover{transform:translateY(-1px);box-shadow:0 4px 14px rgba(0,0,0,.06)}

.address-suggestions{
  position:absolute;
  z-index:30;
  width:min(620px,90vw);
  max-height:260px;
  overflow:auto;
  background:#fff;
  border:1px solid #ddd;
  border-radius:10px;
  box-shadow:0 8px 22px rgba(0,0,0,.12);
}
.address-suggestion{
  padding:10px 12px;
  cursor:pointer;
  border-bottom:1px solid #eee;
}
.address-suggestion:hover{background:#f7eef4}
.address-suggestion:last-child{border-bottom:0}
.module-note{padding:10px;border-radius:8px;background:#fff8dc;border:1px solid #f0d77b}


.field-validation{
  margin-top:5px;
  min-height:16px;
  font-size:12px;
  line-height:1.25;
  font-weight:600;
}
.field-validation.error{color:#b42318}
.field-validation.warning{color:#9a6700}
.field-validation.success{color:#18864b}
.field-has-error{border-color:#d92d20!important;box-shadow:0 0 0 2px rgba(217,45,32,.08)!important}
.field-has-warning{border-color:#d19a00!important;box-shadow:0 0 0 2px rgba(209,154,0,.08)!important}
.field-has-success{border-color:#198754!important}


/* Mantém o alinhamento dos campos mesmo quando surgem mensagens */
.grid > div,
.client-form-grid > div,
.modal .grid > div{
  display:flex;
  flex-direction:column;
  align-self:stretch;
}
.grid > div > input,
.grid > div > select,
.grid > div > textarea,
.client-form-grid > div > input,
.client-form-grid > div > select,
.client-form-grid > div > textarea{
  width:100%;
  box-sizing:border-box;
}
.field-validation{
  margin-top:5px;
  min-height:30px; /* reserva duas linhas para não empurrar os outros campos */
  font-size:12px;
  line-height:1.25;
  font-weight:600;
  display:block;
}


/* Cadastro: mantém o layout original em várias colunas */
#cadastroCliente .client-main-grid{
  display:grid;
  grid-template-columns:repeat(12,minmax(0,1fr));
  gap:10px;
  align-items:start;
}

#cadastroCliente .client-main-grid > div{
  min-width:0;
}

/* Reserva o espaço do aviso desde o início, sem mudar o tamanho da linha */
#cadastroCliente .field-validation{
  min-height:28px;
  height:28px;
  max-height:28px;
  overflow:hidden;
  margin-top:4px;
  font-size:11px;
  line-height:13px;
}

/* Os botões ficam fora do fluxo dos avisos dos campos */
#cadastroCliente > .card > button,
#cadastroCliente > button{
  margin-top:10px;
}

/* Layout responsivo original */
@media(max-width:700px){
  #cadastroCliente .client-main-grid{
    grid-template-columns:1fr;
  }
}


/* ===== Resultado da busca de clientes em cards ===== */
.client-results-grid{
  display:grid;
  grid-template-columns:repeat(auto-fit,minmax(360px,1fr));
  gap:14px;
  margin-top:12px;
}

.client-result-card{
  background:#fff;
  border:1px solid #e8e8ec;
  border-radius:16px;
  padding:16px;
  box-shadow:0 5px 16px rgba(0,0,0,.05);
  transition:.15s ease;
}

.client-result-card:hover{
  transform:translateY(-1px);
  box-shadow:0 8px 20px rgba(0,0,0,.08);
}

.client-result-top{
  display:flex;
  justify-content:space-between;
  gap:12px;